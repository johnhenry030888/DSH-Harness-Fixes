--- pristine/subagent/lib/types/lifecycle.d.ts	2026-09-26 11:27:06.884370970 +0200
+++ installed/subagent/lib/types/lifecycle.d.ts	2026-09-26 11:27:06.865273620 +0200
@@ -25,6 +25,17 @@
 export interface ActivationTerminal {
     /** Why this epoch's last ordinary turn ended, or `error` when teardown failed. */
     readonly stopReason: SubagentResult['stopReason'];
+    /**
+     * Epoch milliseconds at which the terminal state was captured; the stop
+     * moment the settlement notice reports, so "work stopped" and "notice
+     * sent" are distinguishable inside the payload.
+     */
+    readonly stopTime?: number;
+    /**
+     * Epoch milliseconds of the child's own last recorded session activity in
+     * this epoch, when the epoch logged any event.
+     */
+    readonly lastActivityTime?: number;
     /** The epoch's final assistant content, absent when it produced none or failed. */
     readonly output?: ContentBlock[];
 }
