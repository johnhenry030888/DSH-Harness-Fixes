--- pristine/tool-subagent-control/lib/index.js
+++ installed/tool-subagent-control/lib/index.js
@@ -21,7 +21,7 @@
 function apply(ctx) {
 	ctx.tools.register(markAdjacentAgentSendMessageTool(defineTool({
 		name: "send_message",
-		description: "Send a message to a direct continuable child by its agent id. If you are a resident continuable child, you may also target your direct parent. If the target is still working, the message steers its nearest step; if it is idle, the message starts a turn. This call returns no answer from the agent — only confirmation that the message was delivered. A failure means the message was NOT delivered.",
+		description: "Send a message to a direct continuable child by its agent id. If you are a resident continuable child, you may also target your direct parent. If the target is idle, the message starts a turn. If the target is working, the message steers its nearest step, and a steer that arrives while a tool call is in flight cancels that call through the tool-call cancellation signal and replans with your message at the resulting boundary (cancel-then-replan; a tool that ignores its cancellation signal may still run to completion). This call returns no answer from the agent — only confirmation that the message was delivered. A failure means the message was NOT delivered.",
 		parameters: {
 			agent_id: {
 				type: "string",
