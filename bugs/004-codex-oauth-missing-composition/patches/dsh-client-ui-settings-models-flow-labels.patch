--- /tmp/bug004-installed/ui/package/lib/client.js
+++ /tmp/bug004-fixed/ui/package/lib/client.js
@@ -1891,1 +1891,5 @@
 					children: panelState === "loading" ? "Opening secure sign-in..." : panelState === "waiting" ? "Waiting for sign-in..." : panelState === "active" ? "Choose how to sign in." : panelState === "error" ? "Sign-in needs attention." : "Ready to sign in."
+				}), snapshot?.notices?.length > 0 ? (0, react_jsx_runtime.jsx)("p", {
+					className: ModelsSection_module_css_default["fieldLabel"],
+					children: "Step 1: Open the sign-in page"
+				}) : null, flows.map((flow) => (0, react_jsx_runtime.jsxs)("div", {
@@ -1914,1 +1918,1 @@
-					children: [notice.message, notice.url === void 0 ? null : (0, react_jsx_runtime.jsx)("a", { href: notice.url, target: "_blank", rel: "noreferrer", children: " Open sign-in page" }), notice.code === void 0 ? null : ` (${notice.code})`]
+					children: [snapshot?.notices?.length > 0 ? "Waiting for sign-in. " : "", notice.message, notice.url === void 0 ? null : (0, react_jsx_runtime.jsx)("a", { href: notice.url, target: "_blank", rel: "noreferrer", children: " Open sign-in page" }), notice.code === void 0 ? null : ` (${notice.code})`]
